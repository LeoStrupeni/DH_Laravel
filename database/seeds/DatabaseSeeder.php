<?php

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * @return void
     */
    public function run()
    {   
        // Ejercicios: Factories / Seeders

        // Ejercicio 3
        // factory(App\Director::class,10)->create();

        // Ejercicio 4
        // $director = factory(App\Director::class,10)->create();
        // $movies = factory(App\Movie::class,50)->create();

        // foreach ($movies as $movie) {
        //     $movie->director()->saveMany($director->random(1));
        // }
        
        // Ejercicio 6
        // factory(App\Actor::class,50)->create();

        // Ejercicio 7
        // 
    }
}
